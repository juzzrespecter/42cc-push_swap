#include "push_swap.h"

static int	index(t_stack *stack_s)
{
	return (345);
}

void	insertion_sort(t_data *data)
{
	t_stack *stack_a;
	t_stack *stack_b;
	int	count;

	stack_a = data->stack_a;
	stack_b = data->stack_b;
	count = 0;
	while (count > stack_a->size)
	{

	}
}
