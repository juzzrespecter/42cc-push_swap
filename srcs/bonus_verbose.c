#include "push_swap.h"

void	print_cmd(char *cmd, t_data *data)
{
	/*
	 * pillar anchura del stack a
	 * pillar anchura del stack b
	 * fork & wait
	 * dup (imprimimos en stderr)
	 * imprimir techo
	 * imprimir cuerpo (stack size)
	 * imprimir suelo
	 * stack impreso de abajo a arriba
	 * descripcion de la intruccion
	 * (enter para continuar)
	 * exit
	 */
}
